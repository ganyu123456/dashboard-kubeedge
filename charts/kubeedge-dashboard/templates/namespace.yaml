{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "kubeedge-dashboard.namespace" . }}
  labels:
    {{- include "kubeedge-dashboard.labels" . | nindent 4 }}
{{- end }}
